package br.com.nfse.api.service;

public interface NfeService {

    //CARREGAR DTO, COM DADOS DO XML

    // String transmit(NfeReq payload) throws IOException;
    // void cancel(NfeReq payload);
    // void replace(NfeReq payload);
}
